"""AI Math Tutor backend package.

FastAPI app + core engines (SymPy verification, Socratic tutoring), Claude
gateway client, handwriting OCR, auth, and the exam bank. Run from backend/:

    uvicorn app.main:app --host 0.0.0.0 --port 8000
"""
